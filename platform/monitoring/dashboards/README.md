# Monitoring Dashboards
All dashboards should exist within the kustomization for the
application it is for. This directory exists for dashboards
that relate to monitoring the cluster itself or for services
within `core/`, where the CRD will not exist yet.
