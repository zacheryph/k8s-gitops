apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: outline-env
spec:
  refreshInterval: "15s"
  secretStoreRef:
    name: vault
    kind: ClusterSecretStore
  target:
    name: outline-env
  dataFrom:
  - extract:
      key: services/outline
