# this instance is internal to the cluster only
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: internal
spec:
  interval: 1h
  chart:
    spec:
      chart: minio
      version: 8.0.3
      sourceRef:
        kind: HelmRepository
        name: minio
        namespace: flux-system
  values:
    fullnameOverride: internal
    mode: standalone
    accessKey: TJO7WCRFR6UDUGARGOT3NKSX
    secretKey: MyyFXX26c94J1I4vtDqLwrHdT8bJ3S6r-EyX4RNDSaqvvM-n
    buckets:
    - name: registry
      policy: none
    service:
      port: 80
    metrics:
      serviceMonitor:
        enabled: true
