---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pool
  namespace: media
  labels:
    backup.velero.io/schedule: pool
spec:
  accessModes:
  - ReadWriteMany

  resources:
    requests:
      storage: 6Ti

  storageClassName: rook-ceph-fs
