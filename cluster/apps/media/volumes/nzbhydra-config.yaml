---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nzbhydra-config
  namespace: media
  labels:
    snapshot.home.arpa/enabled: "true"
    app.kubernetes.io/name: nzbhydra2
    app.kubernetes.io/instance: nzbhydra2
spec:
  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 2Gi

  storageClassName: rook-ceph-block
