---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: heimdall-config
spec:
  accessModes:
  - ReadWriteOnce

  resources:
    requests:
      storage: 4Gi

  storageClassName: rook-ceph-block
