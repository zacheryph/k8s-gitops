apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: outline
resources:
- postgresql.yaml
- redis.yaml
- release.yaml
- virtualservice.yaml

configMapGenerator:
- name: outline-env
  envs:
  - release.env

generatorOptions:
  disableNameSuffixHash: true
