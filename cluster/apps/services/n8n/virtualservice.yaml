---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: n8n
spec:
  hosts:
  - n8n.${CLUSTER_DOMAIN}
  gateways:
  - system-ingress/gateway
  http:
  - route:
    - destination:
        host: n8n.n8n.svc.cluster.local
        port:
          number: 80
