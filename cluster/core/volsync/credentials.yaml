---
apiVersion: v1
kind: Secret
metadata:
  name: local-credentials
type: Opaque
stringData:
  RESTIC_REPOSITORY: s3:http://minio.volsync-system.svc.cluster.local/volsync
  RESTIC_PASSWORD: ${VOLSYNC_RESTIC_SECRET}
  AWS_ACCESS_KEY_ID: ${VOLSYNC_S3_ACCESS_KEY}
  AWS_SECRET_ACCESS_KEY: ${VOLSYNC_S3_SECRET_KEY}
