apiVersion: config.kio.kasten.io/v1alpha1
kind: Policy
metadata:
  name: bitwarden
spec:
  frequency: '@daily'
  retention:
    daily: 7
    monthly: 12
    weekly: 4
    yearly: 7

  selector:
    matchExpressions:
    - key: k10.kasten.io/appNamespace
      operator: In
      values:
      - bitwarden

  actions:
  - action: backup
    backupParameters:
      filters:
        includeResources:
        - resource: persistentvolumeclaims
  - action: export
    exportParameters:
      frequency: '@daily'
      exportData:
        enabled: true
      migrationToken:
        name: bitwarden-migration-token-f22rp
        namespace: system-kasten
      profile:
        name: offsite
        namespace: system-kasten
    retention: {}
