apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: k8s-triliovault-operator
spec:
  interval: 1h
  chart:
    spec:
      # registryUrl=http://charts.k8strilio.net/trilio-stable/k8s-triliovault-operator
      chart: k8s-triliovault-operator
      version: 2.5.3
      sourceRef:
        kind: HelmRepository
        name: triliovault-operator
        namespace: flux-system
  values:
    installCRDs: true
