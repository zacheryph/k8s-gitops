kafka:
  image:
    registry: docker.io
    repository: bitnami/kafka
    tag: 3.1.0-debian-10-r89
  persistence:
    enabled: true
  replicaCount: 3
  metrics:
    kafka:
      enabled: true
    serviceMonitor:
      enabled: true
      namespace: system-monitor
  kraft:
    clusterId: inwin-development-cluster

kafka-ui:
  fullnameOverride: kafka-ui
  image:
    registry: docker.io
    repository: provectuslabs/kafka-ui
    tag: master
    pullPolicy: Always
  yamlApplicationConfig:
    auth:
      type: disabled
    kafka:
      clusters:
      - name: general
        bootstrapServers: kafka:9092
        metrics:
          port: 9308

addons:
  virtualService:
    name: kafka-ui
    service: authentik
