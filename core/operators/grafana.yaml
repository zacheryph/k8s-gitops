---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: grafana-operator
spec:
  interval: 12h
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 5.21.3@sha256:fa2bf64ee4201d75941a5ae4e1af8d177b08a57c6cfc1d82a6890e8fb202da42
  url: oci://ghcr.io/grafana/helm-charts/grafana-operator
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana-operator
spec:
  interval: 15m
  chartRef:
    kind: OCIRepository
    name: grafana-operator
  timeout: 20m
  install:
    crds: CreateReplace
    disableWait: true
    remediation:
      retries: 5
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 8
      remediateLastFailure: true

  values:
    logging:
      encoder: json
    serviceMonitor:
      enabled: true
    dashboard:
      enabled: true
