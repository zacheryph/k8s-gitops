metallb:
  existingConfigMap: config
  prometheus:
    serviceAccount: prometheus-operator
    namespace: system-monitor
    podMonitor:
      enabled: true
  controller:
    enabled: true
    resources:
      limits:
        cpu: 100m
        memory: 100Mi
  speaker:
    resources:
      limits:
        cpu: 100m
        memory: 100Mi
